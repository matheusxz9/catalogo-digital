services:
  - type: web
    name: catalogo-bella-mizi-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python migrate.py && python -m uvicorn main:app --host 0.0.0.0 --port $PORT
